# Windows (Electron) app

## Run
```bash
cd windows_electron
npm install
# Backend must be running, or set BACKEND_URL to your deployed link
npm start
```

## Build installer
```bash
npm run dist
```

Output is in `dist/` (EXE/installer).
